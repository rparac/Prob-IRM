defaults:
  - cookie/agents@_agent1_config: agent1
  - cookie/shared@_shared_config

local:
  env1: ${.._agent1_config.env}

shared: 
  env: ${.._shared_config.env}

agents:
  A1: ${.._agent1_config.agent}
