defaults:
  - buttons/agents@_agent1_config: agent1
  - buttons/agents@_agent2_config: agent2
  - buttons/agents@_agent3_config: agent3
  - buttons/shared@_shared_config

local:
  env1: ${.._agent1_config.env}
  env2: ${.._agent2_config.env}
  env3: ${.._agent3_config.env}

shared: 
  env: ${.._shared_config.env}

agents:
  A1: ${.._agent1_config.agent}
  A2: ${.._agent2_config.agent}
  A3: ${.._agent3_config.agent}
