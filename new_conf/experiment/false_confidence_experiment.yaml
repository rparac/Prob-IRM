# @package _global_
defaults:
  - override /hydra/sweeper: optuna
  - override /hydra/sweeper/sampler: grid
  # - override /hydra/launcher: joblib # waits for execution of all to complete before starting new processes


# Experiment description
# Try out sensor_false_confidence values only for coffee

hydra:
  sweeper:
    direction: maximize
    study_name: tuning
    storage: null
    n_trials: 5
    n_jobs: 1
    params:
      env.coffee_label_factory.sensor_false_confidence: choice(0.6, 0.7, 0.8, 0.9, 1)

run:
  no_display: True
  name: "false_confidence_experiment"